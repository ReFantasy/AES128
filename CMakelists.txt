cmake_minimum_required(VERSION 2.4 FATAL_ERROR)
PROJECT(AES)
SET(CMAKE_CXX_FLAGS "-std=c++11")



AUX_SOURCE_DIRECTORY(. SOURCE)
add_executable(AES ${SOURCE})
#target_link_libraries(dcgan ${TORCH_LIBRARIES})

